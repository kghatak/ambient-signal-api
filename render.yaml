services:
  - type: web
    name: ambient-signal-api
    runtime: python
    plan: free
    pythonVersion: "3.11.4"
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
